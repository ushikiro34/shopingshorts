# Phase 4 Checklist

## 썸네일
- [ ] 1. 완성 mp4 1개로 generate-thumbnail 호출 시 이미지 생성, 훅 텍스트가 실제로 렌더링됨(AI 생성 이미지가 아니라 실사진 기반인지 코드로 확인)

## 게시 안전장치 (절대규칙 4 검증 — 가장 중요)
- [ ] 2. queue-upload 호출 시 upload_queue 레코드가 `pending_review` 상태로 생성되고 `ready_at`이 현재시각+cooldown_minutes로 정확히 설정된다
- [ ] 3. `ready_at` 이전에는 `/publish` 호출 시 400/403 등 명확한 거부 응답이 오고 실제 업로드가 발생하지 않는다
- [ ] 4. `ready_at` 경과 후 내부 워커가 상태를 `ready_to_publish`로 전환하지만, **이 전환 시점에 유튜브 API가 호출되지 않는다** (워커 로그·네트워크 호출 확인으로 검증)
- [ ] 5. `ready_to_publish` 상태에서 `/api/upload-queue/{id}/publish`를 사람이 호출해야만 실제 유튜브 업로드가 실행되고, 성공 시 `published`+`youtube_video_id`가 기록된다
- [ ] 6. 냉각기간 중 `/cancel` 호출 시 `canceled`로 전환되고, 이후 `/publish` 호출은 거부된다
- [ ] 7. 타이머 경과만으로(사람의 명시적 호출 없이) 실제 유튜브 업로드가 발생하는 코드 경로가 없다 — 코드베이스 전체 검토

## 정책 모니터링
- [ ] 7b. `POST /api/policy-check/run` 호출 시 `MONITORED_POLICIES` 각 항목을 fetch해 policy_snapshots.content_hash를 갱신한다
- [ ] 7c. 정책 페이지 내용을 인위적으로 변경(모의 응답)한 뒤 재실행하면 policy_alerts가 생성되고, 변경 없으면 생성되지 않는다
- [ ] 7d. `GET /api/policy-alerts?reviewed=false`가 미검토 알림만 반환하고, `/review` 호출 후 목록에서 사라진다

## 로그인/대시보드/탭 구조
- [ ] 8b. 로그인 없이 임의 라우트 접근 시 로그인 화면으로 리다이렉트된다
- [ ] 8c. 올바른 계정으로 로그인 시 대시보드 홈으로 이동하고, 세션 쿠키가 httpOnly로 설정된다
- [ ] 8d. 대시보드 홈의 요약 카드 4개 숫자가 실제 DB 상태 카운트와 일치한다
- [ ] 8e. 4개 탭 모두 언제든 클릭 가능하다(비활성화/잠금 없음) — 소프트 게이팅 확인
- [ ] 8f. 특정 탭의 큐가 비어있을 때 안내 배너가 뜨고, 클릭 자체는 막히지 않는다
- [ ] 8g. 어떤 탭에서 [완료하고 다음으로]를 눌러도 항목이 다음 탭 큐에 자동으로 나타난다(탭 강제 이동은 없음, 토스트 안내만)

## 대시보드/배포
- [ ] 8. 배포 URL `/health` 200, 웹+렌더워커+게시상태워커 모두 기동
- [ ] 9. 프로덕션 E2E: 로그인→발굴(80점 이상만 노출 확인)→리뷰입력→분석→대본생성→승인→렌더→썸네일→queue-upload까지 UI로 완료
- [ ] 10. 게시 검토 화면에서 냉각기간 중엔 남은 시간이, 경과 후엔 [게시] 버튼과 경고 문구가 정확히 표시된다
- [ ] 10b. 미검토 정책 알림이 있을 때 대시보드 상단 배너가 표시된다
- [ ] 11. 모바일 뷰포트 390px에서 3단 레이아웃이 화면 전환식(좌→중앙 풀스크린, '← 목록' 복귀)으로 정상 동작한다
- [ ] 12. runbook에 게시 검토 운영법, 정책 알림 대응 절차, failed 복구 절차가 있다
- [ ] 13. 화면 구성·네비게이션·상태별 표시가 `docs/04_ui_spec.md`와 일치한다 (특히 게시 확인 다이얼로그, educational_note 카드 구분 표시)

## 판정
전 항목 통과 → v2.1 완료. 특히 2~7번(게시 안전장치)은 하나라도 실패하면 다른 항목과 무관하게 반려.
